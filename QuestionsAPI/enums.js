const Reviewed = {
  IGNORE: 'IGNORE',
  ONLY: 'ONLY',
  EXCEPT: 'EXCEPT'
};

const OnSystem = {
  IGNORE: 'IGNORE',
  ONLY: 'ONLY',
  EXCEPT: 'EXCEPT'
};

Object.defineProperty(this, 'Reviewed', { enumerable: false, writable: false })();
Object.defineProperty(this, 'OnSystem', { enumerable: false, writable: false })();
